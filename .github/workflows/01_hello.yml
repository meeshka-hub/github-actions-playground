name: Hello

on: 
  push:
    branches:
      - main

jobs:
  hello:
    runs-on: ubuntu-latest

    steps: 
      - uses: actions/checkout@v4

      - name: Print Greeting
        run: |
          echo "Hello, Github Actions!"
          date
          pwd
          ls -la